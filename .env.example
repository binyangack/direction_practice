# 复制本文件为项目根目录下的 .env，把下面的值替换成你自己的 key。
# .env 已被 .gitignore 忽略，不会提交到 git（安全）。config.py 会自动加载它。
# 真实环境变量优先于 .env 文件。

# 必填：DeepSeek API key。去 https://platform.deepseek.com 领取。
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 选填：Tavily 搜索 API key。去 https://tavily.com 注册领取。
# 不设置时 Research 会回退到记忆/估算值（搜索功能停用）。
TAVILY_API_KEY=tvly-yyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyy

# 可选：模型名（默认 deepseek-v4-flash-vision-exp）与 DeepSeek API 地址（默认 https://api.deepseek.com）
# Q3_MODEL=deepseek-v4-flash-vision-exp
# Q3_BASE_URL=https://api.deepseek.com
